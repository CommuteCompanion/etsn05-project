window.base = window.base || {};

window.base.manageUsersController = (() => {
    var model = {
        users: []
    };

    var view = {

    };

    const controller = {
        load: () => {
            alert('test');
        }
    };

    return controller;
});